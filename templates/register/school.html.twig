{% extends 'base.html.twig' %}

{% block title %}Form - Register - School{% endblock %}

{% block body %}
    <!-- school -->
    <section class="section bg-gray">
        <div class="container">
            <div class="row ">
                <div class="col-lg-7 mb-4 mb-lg-0">
                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <div class="form-group">
                        <button type="submit" class="btn btn-success">Register</button>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <!-- /school -->
{% endblock %}